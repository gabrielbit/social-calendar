# Supabase (service role — solo backend/worker)
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_SERVICE_ROLE_KEY=

# AES-256-GCM: 32 bytes en hex (64 caracteres). Mismo formato que apps/api.
TOKEN_ENCRYPTION_KEY=

# Worker
WORKER_POLL_INTERVAL_MS=15000
WORKER_BATCH_SIZE=10
LOG_LEVEL=info
