[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @agenda/worker build"

[deploy]
startCommand = "pnpm --filter @agenda/worker start"
restartPolicyType = "ON_FAILURE"
